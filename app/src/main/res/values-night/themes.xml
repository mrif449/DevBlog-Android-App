<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DevBlog" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="android:statusBarColor">@color/your_dark_status_bar_color</item>
        <item name="android:windowLightStatusBar">false</item> <!-- Dark icons -->
    </style>

    <style name="Theme.DevBlog" parent="Base.Theme.DevBlog" />
</resources>